[package]
name = "oxidust"
version = "0.1.0"
edition = "2021"

[dependencies]
slint = { version = "1.9", features = ["unstable-winit-030"] }
chrono = "0.4"

[target.'cfg(windows)'.dependencies]
winreg = "0.52"

[build-dependencies]
slint-build = "1.9"
